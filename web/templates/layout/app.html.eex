<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Notex!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <header class="layout-header">
      <nav role="navigation" class="header">
        <a class="logo" href="/">Notex</a>

        <ul class="header-right">
          <%= if logged_in?(@conn) do %>
            <li><%= current_user(@conn).email %></li>
            <li><%= link "Logout", to: session_path(@conn, :delete), method: :delete %></li>
          <% else %>
            <li>
                <i class="iconfont icon-search"></i>
            </li>
            <li>
                <%= link to: session_path(@conn, :index) do %>
                    <i class="iconfont icon-login"></i>
                <% end %>
            </li>
          <% end %>
        </ul>
      </nav>
    </header>

    <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
    <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>

    <main role="main">
      <%= render @view_module, @view_template, assigns %>
    </main>

  </body>
</html>
